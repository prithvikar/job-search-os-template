# Constraints (hard filters)

comp_floor_usd: 0          # walk-away base/total
locations: []              # acceptable; [] = open
remote_ok: true
visa_needs: none
dealbreakers: []           # e.g. ["no on-call", "no relocation"]
target_titles: []          # e.g. ["AI Product Manager", "Senior PM"]
